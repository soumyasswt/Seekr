fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# No other dependencies — live search uses Python stdlib only (urllib, html.parser)
# Optional: install these for crawling local websites
# requests>=2.31.0
# beautifulsoup4>=4.12.0
